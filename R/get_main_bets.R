# get_main_bets <- function(site, sport, save_path = NULL) {
#
#   if (site %in% ('dk', 'draftkings')) {
#     dk_data <- get_draftkings_data(sport, save_path = save_path)
#     if (sport == 'nba') {
#
#
#     }
#     if (sport == 'mlb') {
#       y=z
#     }
#   }
# }
